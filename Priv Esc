Check for SUID's; can also check the /opt directory; look for newer files
(VICTIM) find / -perm -u=s -type f 2>/dev/null | xargs ls -l

cd /opt

ls

./admin

(upgrade your shell)
/bin/bash

(fix size of your TERM)
export TERM=xterm-256color

(ATTACKER) echo $TERM && tput lines && tput cols 
(VICTIM) stty rows 38 columns 116 
